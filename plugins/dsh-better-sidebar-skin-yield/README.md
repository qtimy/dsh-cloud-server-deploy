# dsh-better-sidebar-skin-yield

Moves dsh-better-sidebar's top-right controls below fake-window skin title
bars. The selector matches the stable CSS-module suffix `_toggleCluster`
instead of a generated hash, so sidebar rebuilds do not silently break it.
